
import 'package:flutter/material.dart';

class NavItem {
  String title;
  Widget icon;
  Widget iconSelected;

  NavItem({
    required this.title,
    required this.icon,
    required this.iconSelected,
  });
}
